<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="mattermost_webhooks.list">
      <field name="name">mattermost_webhooks list</field>
      <field name="model">mattermost_webhooks.mattermost_webhooks</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="mattermost_webhooks.action_window">
      <field name="name">mattermost_webhooks window</field>
      <field name="res_model">mattermost_webhooks.mattermost_webhooks</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="mattermost_webhooks.action_server">
      <field name="name">mattermost_webhooks server</field>
      <field name="model_id" ref="model_mattermost_webhooks_mattermost_webhooks"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="mattermost_webhooks" id="mattermost_webhooks.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="mattermost_webhooks.menu_1" parent="mattermost_webhooks.menu_root"/>
    <menuitem name="Menu 2" id="mattermost_webhooks.menu_2" parent="mattermost_webhooks.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="mattermost_webhooks.menu_1_list" parent="mattermost_webhooks.menu_1"
              action="mattermost_webhooks.action_window"/>
    <menuitem name="Server to list" id="mattermost_webhooks" parent="mattermost_webhooks.menu_2"
              action="mattermost_webhooks.action_server"/>
    -->
  </data>
</odoo>